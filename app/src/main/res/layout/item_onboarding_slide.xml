<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp"
    android:background="@color/background">

    <!-- Slide Illustration Frame -->
    <FrameLayout
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="40dp">

        <!-- Glowing Aura background -->
        <View
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_logo_gold_glow" />

        <!-- Circular Card Holder -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:layout_gravity="center"
            app:cardBackgroundColor="@color/surface"
            app:cardCornerRadius="70dp"
            app:cardElevation="4dp"
            app:strokeColor="@color/tertiary"
            app:strokeWidth="1.5dp">

            <!-- Center icon -->
            <ImageView
                android:id="@+id/image_slide_icon"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_logo_lumora"
                android:contentDescription="Slide Icon"
                app:tint="@color/primary" />

        </com.google.android.material.card.MaterialCardView>

    </FrameLayout>

    <!-- Slide Title -->
    <TextView
        android:id="@+id/text_slide_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:fontFamily="@font/cinzel"
        android:textColor="@color/primary"
        android:textSize="24sp"
        android:textStyle="bold"
        android:letterSpacing="0.05"
        android:text="Welcome to Lumora" />

    <!-- Divider decoration -->
    <View
        android:layout_width="80dp"
        android:layout_height="1.5dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:background="@color/tertiary" />

    <!-- Slide Description -->
    <TextView
        android:id="@+id/text_slide_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:fontFamily="@font/cormorant_garamond"
        android:textColor="@color/text_secondary"
        android:textSize="18sp"
        android:lineSpacingMultiplier="1.2"
        android:text="Where Ancient Wisdom Meets Modern Learning. Enter the sacred archive of academic excellence." />

</LinearLayout>
